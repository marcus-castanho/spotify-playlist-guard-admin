import { Meta } from '@storybook/blocks';
import { Story } from '@/stories/components/Story.tsx';
import { Color, colors } from '@/styles/theme';
import { WarningIcon } from '@/components/icons/WarningIcon.tsx';
import { IconViewer } from '../components/IconViewer.tsx';
import { ExportButtons } from '../components/ExportButtons.tsx';
import { IconInfo } from '../components/IconInfo.tsx';
import { renderToString } from 'react-dom/server';

<Meta title="Foundations/Icons/warning" />

<Story title={'warning'}>
    <IconViewer>
        <WarningIcon size={12} />
    </IconViewer>
    <ExportButtons
        iconName={'warning'}
        componentCode={renderToString(<WarningIcon size={12} />)}
    />
    <IconInfo>
        ```js
        <WarningIcon size={12} fillColor="black" />
        ```
    </IconInfo>
</Story>
